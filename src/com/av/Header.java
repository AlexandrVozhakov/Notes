package com.av;import javax.swing.*;import java.awt.*;/** * Created by av on 20.01.16. */public class Header extends JLabel {    //Controller controller = Model.getControl();    public Header(){        String date = Program.date("HH:mm");        this.setText("<html><b>New note</b><br/><FONT color=\"#808080\">" + date + "</FONT></html>");    }    public void setHeaderName(String text){        //if(maxLengthCheck(text, 330)){            //text = text.substring(0, text.length() - 2) + "...";        //}        String date = Program.date("HH:mm");        this.setText("<html><b>" + text + "</b><br/><FONT color=\"#808080\">" + date + "</FONT></html>");    }    private boolean maxLengthCheck(String text, int length){        FontMetrics fontMetrics = this.getFontMetrics(Program.globalFont);        int text_width = fontMetrics.stringWidth(text);        if(text_width > length){            return false;        }        return true;    }    public String toString(){        return this.getText();    }}